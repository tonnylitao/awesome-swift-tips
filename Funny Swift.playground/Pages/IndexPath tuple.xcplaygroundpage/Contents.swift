//: [Previous](@previous)

import Foundation
import UIKit

extension IndexPath {
    
    var tuple: (Int, Int) {
        (section, row)
    }
}

//let (sec, row) = indexPath.tuple

//: [Next](@next)
